// Domain models (device models, device instances, parameters, session/event history)
// are added in issue #5 — this scaffold only wires up Prisma + Postgres.

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
